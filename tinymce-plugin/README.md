# TinyMCE "Zavrad" Plugin

## Build:

```
yarn build
```

## Launch example:

Using `http-server` library a demo can plugin demo can be launched.

```
yarn build
npm i http-server
cd src/demo/html
http-server
```